# Вопросы для проверки понимания проекта

## Блок 0 — Базовые понятия (фундамент)

### 0.1. Контейнеризация: Docker

1. Что такое Docker? Зачем он нужен в разработке? Чем контейнер отличается от виртуальной машины?
2. Что такое Docker-образ (image) и Docker-контейнер (container)? Приведи аналогию из реального мира.
3. Что делает команда `docker-compose up`? Почему она удобнее трёх отдельных `docker run`?
4. Что такое `docker-compose.yml`? Какие три сервиса в нём описаны в нашем проекте?
5. Что значит «пробросить порт»? Зачем в проекте порты `5432:5432` и `3000:80`?

### 0.2. Веб-приложения: клиент-сервер

6. Что такое клиент-серверная архитектура? Кто в нашем проекте клиент, а кто сервер?
7. Что такое HTTP? Какие HTTP-методы ты знаешь и для чего они?
8. Что такое URL? Разбери на части: `http://localhost:3000/api/utilities/sum-numbers/execute`
9. Что такое API? Чем REST API отличается от обычного веб-сайта?
10. Что такое JSON? Почему именно его используют для обмена данными, а не XML или обычный текст?

### 0.3. Backend: C# и ASP.NET Core

11. Что такое ASP.NET Core? Зачем нужен фреймворк, если можно написать всё на чистом C#?
12. Что такое Dependency Injection (внедрение зависимостей)? Объясни «на пальцах».
13. Что такое Singleton, Scoped и Transient? В чём разница? Приведи пример из проекта.
14. Что такое интерфейс (interface) в C#? Зачем он нужен? Пример из проекта.
15. Что такое контроллер (Controller)? Какую роль он выполняет в ASP.NET Core?

### 0.4. База данных

16. Что такое база данных? Зачем она нужна в веб-приложении?
17. Что такое SQL? Чем реляционная БД (PostgreSQL) отличается от нереляционной (MongoDB)?
18. Что такое ORM? Зачем нужен Entity Framework Core, если можно писать SQL-запросы вручную?

### 0.5. Frontend: JavaScript и браузер

19. Что такое DOM? Как JavaScript взаимодействует с HTML-страницей?
20. Что такое событие (event) и обработчик события? Пример из нашего проекта.
21. Что такое AJAX / fetch? Почему страница не перезагружается при запросе данных с сервера?
22. Что такое Promise и async/await? Зачем они нужны? Пример из `api.js`.
23. Что такое SPA (Single Page Application)? Чем оно отличается от многостраничного сайта?

### 0.6. Инфраструктура: Nginx

24. Что такое веб-сервер? Зачем нужен Nginx, если ASP.NET Core сам умеет принимать HTTP-запросы?
25. Что такое reverse proxy? Какую роль он выполняет в нашем проекте?
26. Что такое CORS? Почему браузер блокирует запросы к «чужому» серверу?
27. Что такое Swagger / OpenAPI? Какую пользу он приносит разработчику?
28. Что такое environment (окружение): Development, Staging, Production? Как в проекте разделены настройки для разработки и продакшена?

### 0.7. Git и командная работа

29. Что такое система контроля версий? Зачем она нужна, если я работаю один?
30. Что такое Git? Чем он отличается от GitHub?
31. Что такое коммит (commit)? Что значит «хороший коммит»?
32. Что такое ветка (branch)? Зачем нужна ветка `feature/hash-calc`?
33. Что такое Pull Request (PR) и code review? Зачем ревьювить код, если «оно работает»?

### 0.8. Безопасность (общий кругозор)

34. Что такое HTTPS? Чем он отличается от HTTP? Нужен ли он на `localhost`?

### 0.9. Тестирование и качество кода

35. Что такое unit-тест? Зачем его писать, если можно проверить «руками»?
36. Что такое «чистый код»? Назови три признака.
37. Что такое рефакторинг? Чем он отличается от исправления бага?

---

## Блок 1 — Общая архитектура

### 1.1. База
38. Из каких трёх основных слоёв состоит приложение? Опиши роль каждого.
39. Что такое SPA (Single Page Application) и как это реализовано в нашем проекте?
40. Чем REST API отличается от классического серверного рендеринга страниц?
41. Почему frontend и backend живут в **разных Docker-контейнерах**, а не в одном?

### 1.2. Средний уровень
42. Нарисуй на доске схему: что происходит от момента ввода `http://localhost:3000` в браузере до отображения списка утилит. Какие сервисы задействованы? В каком порядке?
43. Почему API-запросы с фронтенда идут на `/api/...`, а не на `http://api:8080/api/...`? Где и как происходит подмена адреса?
44. Что такое CORS? Почему в проекте он настроен разрешающим всё (`AllowAnyOrigin`)? В чём опасность такой настройки в production?

### 1.3. Сложный
45. Представь, что нужно развернуть приложение на продакшен-сервере с доменом `utils.company.ru` и HTTPS. Что нужно изменить в: `docker-compose.yml`, `nginx.conf`, `Program.cs`? Перечисли конкретные строки.
46. Как обеспечивается отказоустойчивость приложения? Что произойдёт, если контейнер `api` упадёт? А если `postgres`?

---

## Блок 2 — Backend (ASP.NET Core)

### 2.1. База
47. Что делает `Program.cs`? Перечисли ВСЕ шаги, которые выполняются при старте приложения.
48. Что такое Dependency Injection (DI)? Как оно используется в проекте? Приведи конкретный пример из кода.
49. Зачем нужен интерфейс `IUtilityService`? Почему нельзя просто наследоваться от базового класса?
50. Объясни маршрут `[Route("api/[controller]")]` в `UtilitiesController`. Какой URL соответствует методу `GetAll()`?
51. Что такое DTO и зачем они используются? Приведи пример DTO из проекта.

### 2.2. Средний уровень
52. Как работает `UtilityOrchestrator`? Почему он зарегистрирован как **Singleton**, а не как Scoped или Transient?
53. Опиши полный путь запроса `POST /api/utilities/sum-numbers/execute` от получения до ответа. Какие классы задействованы?
54. Что такое Entity Framework Core? Какую роль в проекте выполняет `AppDbContext`?
55. В чём разница между `EnsureCreated()` и `Migrate()` в EF Core? Почему в проекте используется `EnsureCreated()`?
56. Как связаны таблицы `Utilities` и `ExecutionResults` в базе данных? Как эта связь описана в коде?

### 2.3. Сложный
57. Что произойдёт при одновременном вызове утилиты «Сумма чисел» из десяти вкладок браузера? Есть ли риск гонки данных? Обоснуй.
58. Как организовать **фоновую задачу** (например, очистку истории выполнений старше 30 дней)? Куда добавить код?

---

## Блок 3 — Frontend (JavaScript SPA)

### 3.1. База
59. Как организована навигация между страницами без перезагрузки? Что такое хэш-роутер?
60. Объясни, как работает `Router.navigate()`. Что произойдёт, если ввести несуществующий хэш?
61. Что делает `DashboardPage.render()`? Какие данные он запрашивает у backend и как их отображает?
62. Как frontend узнаёт, реализована утилита или нет? Где это свойство обновляется?
63. Зачем нужна функция `UtilityPage.escape()`? Что будет, если её убрать?

### 3.2. Средний уровень
64. Почему скрипты подключены в `<body>` без `defer`/`async`? Что изменится, если добавить?
65. Как фильтрация на дашборде работает без повторного запроса к backend? В чём плюсы и минусы такого подхода?
66. В `dashboard.js` кнопки фильтров создаются динамически. Почему обработчики `addEventListener` добавляются **после** вставки HTML в DOM?

### 3.3. Сложный
67. Как добавить новую страницу (например, «Настройки»)? Опиши пошагово: какие файлы создать, что изменить в `app.js` и `index.html`.

---

## Блок 4 — База данных (PostgreSQL + EF Core)

### 4.1. База
68. Почему выбрана PostgreSQL, а не SQLite или SQL Server? Назови два преимущества для этого проекта.
69. Что такое seed-данные? Где они описаны в проекте и зачем?
70. Как в `AppDbContext` описана связь «один-ко-многим» между `Utility` и `ExecutionResult`? Покажи строку кода.
71. Где хранятся учётные данные БД? В каком файле и почему он не committится в Git?

### 4.2. Средний уровень
72. Что произойдёт, если удалить том `postgres_data` командой `docker-compose down -v` и запустить проект заново? Какие данные потеряются, а какие восстановятся?
73. Как работает `connection string` `Host=postgres;Port=5432;...`? Почему host называется `postgres`, а не `localhost`?
74. В контроллере есть метод `GetHistory` с параметром `limit`. Какой SQL-запрос сгенерирует EF Core для этого метода?

### 4.3. Сложный
75. Как добавить **пагинацию** в список утилит (например, по 10 на страницу)? Что изменится в контроллере и на фронтенде?
76. Представь, что история выполнений разрослась до миллионов записей и страница `/history` стала тормозить. Предложи два способа оптимизации.

---

## Блок 5 — Docker и инфраструктура

### 5.1. База
77. Объясни, что делает каждая строка в backend-`Dockerfile`. Почему сборка разделена на два этапа (`AS build` и `AS runtime`)?
78. Что делает `healthcheck` у сервиса `postgres` в `docker-compose.yml`? Что бы произошло без него?
79. Как контейнеры общаются друг с другом? Что такое docker-сеть `intern-net`?

### 5.2. Средний уровень
80. Почему порт PostgreSQL (`5432:5432`) прокинут наружу? Нужно ли это в production? Обоснуй.
81. Что произойдёт при выполнении `docker-compose up -d --build`? Опиши по шагам, в каком порядке запускаются сервисы.
82. Где в проекте используется `.dockerignore`? Зачем он нужен? Что будет, если его убрать?

### 5.3. Сложный
83. Как уменьшить размер backend-образа? Предложи минимум два способа, не ломая функциональность.
84. Как организовать **HTTPS** для localhost-разработки в Docker? Что нужно добавить в `docker-compose.yml` и `nginx.conf`?

---

## Блок 6 — Git и рабочий процесс

### 6.1. База
85. Для чего нужен `.gitignore`? Какие три типа файлов он исключает в нашем проекте?
86. Что такое feature-branch workflow? Как бы ты назвал ветку для реализации утилиты «Хеш-калькулятор»?
87. В чём разница между `git merge` и `git rebase`? Какой подход используется в нашем процессе?

### 6.2. Средний уровень
88. Что делать, если твой Pull Request получил замечания на code review? Опиши пошаговый процесс исправления.
89. Как разрешить конфликт слияния (merge conflict)? Приведи пример ситуации, в которой он может возникнуть в нашем проекте.

---

## Блок 7 — Разработка утилит (практика)

### 7.1. База
90. Какие три шага нужно выполнить, чтобы добавить новую утилиту в проект? Назови конкретные файлы.
91. Что делает метод `Execute(string input)`? Какие данные он принимает и что возвращает?
92. Зачем проверять `string.IsNullOrWhiteSpace(input)` в начале утилиты? Что будет, если этого не делать?

### 7.2. Средний уровень
93. Утилита «Множественная замена в тексте» принимает JSON. Как распарсить JSON внутри `Execute()`? Какой NuGet-пакет или встроенный класс использовать?
94. Как написать unit-тест на утилиту «Сумма чисел»? Напиши псевдокод трёх тест-кейсов: нормальный ввод, пустой ввод, ввод с некорректными данными.
95. Если утилите нужен специфичный UI (например, Color Converter с палитрой), как его добавить? Назови файлы, которые нужно изменить/создать.

### 7.3. Сложный
96. Представь, что утилита выполняет **долгую операцию** (10+ секунд) — например, обработку большого CSV-файла. Как не заставлять пользователя ждать? Предложи архитектурное решение (async, WebSocket, polling).
97. Как ограничить доступ к утилите по IP-адресу (чтобы ей могли пользоваться только сотрудники из офисной сети)? Где добавить эту проверку: в контроллере, middleware или на уровне nginx? Обоснуй.

---

## Блок 8 — Безопасность (общий кругозор)

98. Какие уязвимости могут возникнуть, если утилита принимает пользовательский ввод и вставляет его в SQL-запрос? Как в нашем проекте это предотвращено?
99. Что такое XSS? Есть ли защита от XSS в проекте? Покажи конкретную строку кода в `utility.js`.
100. Зачем нужен `X-Forwarded-*` заголовки в `nginx.conf`? Какая уязвимость возникает, если их не передавать?
101. Пароль от БД (`postgres:postgres`) хранится в `appsettings.json`. Почему это допустимо для учебного проекта, но недопустимо в production? Как правильно хранить секреты?

---

## Итог по списку вопросов

Всего **101 вопрос** (37 базовых понятий + 64 по архитектуре проекта).

1. **До начала практики / День 1**: вопросы из Блока 0 (базовые понятия) — понимание фундамента: Docker, HTTP, клиент-сервер, Git.
2. **День 1-2**: блоки 1.1, 2.1, 3.1, 4.1, 5.1 — базовая архитектура проекта.
3. **День 5 (середина практики)**: блоки 1.2, 2.2, 3.2, 4.2, 6.1 — после первой реализованной утилиты.
4. **День 10 (завершение практики)**: блоки 1.3, 2.3, 3.3, 4.3, 5.3, 7.3, 8 — сложные вопросы для глубинного понимания.
